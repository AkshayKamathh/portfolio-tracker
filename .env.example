# Required to hit a real database (Atlas or other MongoDB).
# mongodb+srv://USER:PASS@cluster.example.mongodb.net/?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://stefanle_db_user:stefan@cluster0.j8mqwr4.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

MONGO_DB_NAME=portfolio_tracker

# CORS: one origin or comma-separated. If you use Vite on localhost or 127.0.0.1, both are allowed automatically.
FRONTEND_ORIGIN=http://localhost:5173

VITE_API_BASE_URL=http://localhost:8000

# Optional: Yahoo Finance cache TTL in seconds (default 60).
# YF_CACHE_TTL=60
